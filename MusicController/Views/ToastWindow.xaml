<Window x:Class="MusicController.Views.ToastWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Toast" 
        Width="300" Height="80"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize">
    <Border Background="#E0202020" 
            CornerRadius="8" 
            BorderBrush="#404040" 
            BorderThickness="1"
            Margin="5">
        <Border.Effect>
            <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.5"/>
        </Border.Effect>
        <StackPanel VerticalAlignment="Center" Margin="15,8">
            <TextBlock x:Name="ActionText"
                       Text="⏯️ Oynat/Duraklat"
                       Foreground="#888888"
                       FontSize="11"/>
            <TextBlock x:Name="SongText"
                       Text="Şarkı Adı"
                       Foreground="White"
                       FontSize="14"
                       FontWeight="SemiBold"
                       TextTrimming="CharacterEllipsis"/>
            <TextBlock x:Name="ArtistText"
                       Text="Sanatçı"
                       Foreground="#4CAF50"
                       FontSize="12"
                       TextTrimming="CharacterEllipsis"/>
        </StackPanel>
    </Border>
</Window>
